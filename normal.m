% Alunos: André, Karin e Simon
% Normaliza os dados passados

function grupoNormal = normal(grupo, j)
  %Fazer normalização de um atributo (coluna)
  %x_normalizado = x - min(x) / max(x) - min(x)  
endfunction